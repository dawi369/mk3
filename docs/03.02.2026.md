spread vs overlay easy switch in chart, also reverse spread

Figure out rolling + icon

protect frontend {billing, settings} and backend rounts (redis, jobs)

do a small recording for nate

protect client-redis ws connection

figure out flow of entire website for onboarding

notificaitons?

chart is continuous even with big gaps in time, fix this

drawer close anymations isnt like default, same with sidebar

drawing new candles or something resets the view back to the front when trying to scroll back

need to encode more information into the volumne and VWAP, like indicators that tell you when to trade

automate backend redis

curl -X POST http://localhost:3001/admin/refresh-front-months -H "X-API-Key: Letadlo68"

curl -X POST http://localhost:3001/admin/snapshot_job -H "X-API-Key: Letadlo68"

docker exec -it mk3-redis redis-cli