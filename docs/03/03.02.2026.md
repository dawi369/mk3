shift click two for overlay

spread vs overlay easy switch in chart, also reverse spread

add .contract-row {
  user-select: none; /* Prevents text highlighting */
  cursor: default;   /* Keeps the mouse pointer normal (not the 'I' beam) */
} to prevent text highliting with shift click